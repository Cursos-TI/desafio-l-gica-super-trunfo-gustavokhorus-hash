#include <stdio.h>
    int main(){
        
        for(int i = 1; i <= 10; i++){
        printf("%d\n", i);
        }

        return 0;
    }

    /*
    Executa o código de acordo com a quantidade de repetição desejada.
    for(inicialização; condição; incremente ou decremento){
    //código
    }
    executa a inicialização; verifica a condição;
    se for verdadeira, executa bloco;
    depois faz o incremento/decremento;
    repete o processo
    */